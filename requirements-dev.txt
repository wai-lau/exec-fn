# Dev/test-only deps — NOT installed in the prod image (keeps it lean).
# Install into a venv to run the smoke suite:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/pytest tests/ -q
pytest
httpx
# WebKit browser tests (tarot reading-progression). Marked `browser`: skipped by
# the general smoke step, but pre-commit runs them as a dedicated step when the
# tarot reader changes. After install, fetch the browser:
#   .venv/bin/playwright install webkit
#   .venv/bin/pytest tests/test_tarot_progression.py -q
playwright
